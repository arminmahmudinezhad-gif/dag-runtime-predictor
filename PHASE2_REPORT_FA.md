# گزارش فاز ۲ — کالیبراسیون، تعمیم‌پذیری و زمان‌بندی بی‌درنگ
### پیش‌بینی چندکی زمان اجرای گره‌های DAG روی بستر چندهسته‌ای ناهمگن + DVFS

**درس:** سامانه‌های بی‌درنگ (Real-Time Systems) — نیمسال دوم ۰۴-۰۵
**پروژه:** پروژه اول — پیش‌بینی توزیعی زمان اجرای گره‌های DAG روی بستر ناهمگن
**فاز:** ۲ از ۲ — کالیبراسیون هم‌ریخت، تعمیم‌پذیری در برابر OOD/مقیاس‌پذیری، و تأثیر بر زمان‌بندی بی‌درنگ
**مبنای تحویل:** `RealTime_Systems_Project1_4042_Sharif.pdf` (مشخصات رسمی پروژه) + `Phase_2_Handoff_Guide_DAG_Execution_Time_Project.pdf` (قرارداد تحویل مصنوعات منجمدشده‌ی فاز ۱)

---

## ۰. دامنه و یک قاعده‌ای که بر همه‌ی موارد زیر حاکم است

> **پیش‌بین فاز ۱ منجمد (frozen) است.** تمام نتایج این گزارش با بارگذاری
> `models/phase1/checkpoints/best_model.pt` و وضعیت پیش‌پردازش دقیقاً برازش‌شده روی داده‌ی
> آموزش، از روی دیسک، با `strict=True` برای بارگذاری state-dict و `model.eval()` تولید شده‌اند.
> **هیچ وزنی هرگز به‌روزرسانی نمی‌شود، هیچ مقیاس‌کننده‌ای هرگز دوباره برازش نمی‌شود، و نه مجموعه‌ی
> کالیبراسیون و نه مجموعه‌های test-ID/test-OOD برای انتخاب مدل استفاده نمی‌شوند.** تنها *مدل‌های*
> جدیدی که در سرتاسر فاز ۲ آموزش داده شده‌اند، روش‌های مبنای مقایسه‌ای مستقل در بخش ۵ هستند
> (MLP، GNN-Mean، GNN-Mean-NoZt) — این‌ها شبکه‌های جدید و مجزایی هستند که صرفاً برای یک جدول
> مقایسه‌ای کنار هم استفاده می‌شوند؛ هرگز پیش‌بین منجمد را تغییر نمی‌دهند یا جایگزین آن نمی‌شوند.

**بررسی صحت مدل منجمد** (بخش ۱۹ راهنمای تحویل، که صراحتاً پیش از آغاز کار فاز ۲ درخواست شده
است): مدل به‌طور کاملاً مستقل در همین محیط بازسازی شد — بارگذاری تازه از دیسک برای
`best_model.pt` + `preprocessing_state.json` از طریق `src/phase1/bootstrap.py` (شامل رفع
مشکل مستندشده‌ی فیلتر کردن کلیدواژه‌های `Phase1Config`)، که در فرایندی مجزا از هر آنچه خط پایه‌ی
ذخیره‌شده‌ی فاز ۱ را تولید کرده اجرا شد. مقایسه‌ی پیش‌بینی‌های خام Q50/Q90/Q95/Q99 این بارگذاری
تازه با خط پایه‌ی از پیش ذخیره‌شده در `results/phase1/predictions/test_id_predictions.csv` روی
تمام ۲٬۱۰۰ رکورد test-ID: **بیشینه‌ی اختلاف مطلق = ۰٫۵ میکروثانیه، میانگین اختلاف مطلق ≈
۰٫۰۳۵ میکروثانیه** — که نویز عددی در سطح ممیز شناور است، نه یک اختلاف معنادار. بازتولیدپذیری
بارگذاری چک‌پوینت منجمد پیش از آنکه هر تحلیل فاز ۲ روی آن بنا شود، تأیید می‌شود.

هر کمیت کالیبره‌شده‌ی `C_e` در این گزارش یک **کران بالای پیش‌بینی‌شده‌ی کالیبره‌شده‌ی آماری با
تضمین پوشش حاشیه‌ای** تحت فرض تبادل‌پذیری بین مجموعه‌ی کالیبراسیون و مجموعه‌ی ارزیابی است،
**نه یک WCET قطعی**. این تمایز در هر نقطه از نتایج زمان‌بندی دوباره بیان می‌شود، زیرا نحوه‌ی
خواندن اعداد را تغییر می‌دهد.

**بازتولید این گزارش از ابتدا تا انتها:**

```bash
python3 scripts/run_phase2_inference.py       # frozen inference: calibration/test_id/test_ood
python3 scripts/run_phase2_calibration.py     # split-conformal calibration by criticality
python3 scripts/run_phase2_ood_structure.py   # OOD + graph-structure sensitivity
python3 scripts/run_phase2_scalability.py     # 4/8/16/32-core stress test
python3 scripts/run_phase2_drift.py           # tail-load / overload / sliding-window calibration
python3 scripts/run_phase2_scheduling.py      # HEFT + deadline-aware scheduling
python3 scripts/run_phase2_baselines.py       # MLP / GNN-Mean / GNN-Mean-NoZt comparison
python3 scripts/run_phase2_deepdive.py        # operation_type, inference-time, GNN-embedding correlation
python3 scripts/run_phase2_plots.py           # all figures below
```

همه‌ی ماژول‌های منبع در `src/phase2/` قرار دارند (`inference.py`، `conformal.py`، `metrics.py`،
`graph_structure.py`، `scalability.py`، `drift.py`، `scheduling.py`، `baselines.py`،
`config.py`)؛ `src/phase1/bootstrap.py` دقیقاً همان‌طور که در راهنمای تحویل مشخص شده مدل منجمد
را بازسازی می‌کند (شامل رفع مستندشده‌ی فیلتر کلیدواژه‌ی `Phase1Config` برای `num_quantiles`).

---

## ۱. تحلیل اثر کالیبراسیون هم‌ریخت
*(بخش مشخصات‌: "تحلیل اثر کالیبراسیون هم‌ریخت"؛ کد: `src/phase2/conformal.py`،
`scripts/run_phase2_calibration.py`)*

### ۱.۱ نقطه‌ی عملیاتی انتخاب‌شده و چرایی آن

| پارامتر | مقدار | توجیه |
|---|---|---|
| `base_quantile` (1-α_h) | **Q95** برای هر دوی HI و LO | Q95 در test-ID خام از قبل بیش از حد پوشش می‌دهد (پوشش ۱٫۰۰۰)، به‌طوری که برای لایه‌ی کالیبراسیون تقریباً چیزی برای اصلاح باقی نمی‌ماند — امتیازهای عدم‌انطباق آن برای تقریباً هر ردیف ≈۰ هستند. Q95 فضای واقعی و داده‌محوری برای انجام اصلاح واقعی توسط کالیبراسیون باقی می‌گذارد، ضمن آنکه همچنان یک چندک بالای مشروع است. |
| `δ_HI` | **۰٫۰۱** (هدف پوشش ۹۹٪) | گره‌های DAG با بحرانیت بالا سخت‌گیرانه‌ترین حاشیه‌ی ایمنی آماری‌ای را که سر آموزش‌دیده می‌تواند پشتیبانی کند دریافت می‌کنند. |
| `δ_LO` | **۰٫۰۵** (هدف پوشش ۹۵٪) | کارهای best-effort حاشیه‌ی کافی اما کمتر محافظه‌کارانه‌ای دریافت می‌کنند تا از تورم غیرضروری بودجه جلوگیری شود. |

**خاستگاه، به‌طور دقیق بیان‌شده تا هرگونه ابهام درباره‌ی نگاه به مجموعه‌ی آزمون رفع شود:**
مقایسه‌ی Q95 در برابر Q99 در بالا از خط پایه‌ی *از پیش موجود* و *از پیش محاسبه‌شده‌ی* فاز ۱ که
در بخش ۱۲ `Phase_2_Handoff_Guide` / `results/phase1/metrics/test_id_metrics.json` تحویل داده
شده می‌آید (پوشش خام @Q95 = ۰٫۹۹۰۰، @Q99 = ۱٫۰۰۰۰) — **نه** از هیچ ارزیابی جدید محاسبه‌شده در
فاز ۲. این نقطه‌ی عملیاتی (`src/phase2/config.py`) *پیش از* آنکه هیچ عدد پوشش کالیبره‌شده‌ی
test_id/test_ood محاسبه‌شده در فاز ۲ دیده شود، ثابت شده است. پویش حساسیت α/δ در بخش ۱.۴
*پس از آن* ارزیابی می‌شود، صرفاً به‌عنوان یک گزارش شفافیت که طبق دستورالعمل مشخصات ("α/δ را
پنهانی اختراع نکنید، حساسیت را گزارش دهید") لازم است — هرگز به این پیش‌فرض بازنمی‌گردد تا آن را
تغییر دهد، بنابراین به معنای تنظیم روی test_id/test_ood نیست (رویه‌ای که چک‌لیست Do/Do-Not
راهنمای تحویل صراحتاً منع می‌کند).

### ۱.۲ وضعیت کالیبراسیون برازش‌شده (روی مجموعه‌ی کالیبراسیون دست‌نخورده‌ی ۵۰-DAG / ۲٬۱۰۰-رکوردی)

| بحرانیت | N | k_h | q_conf (میکروثانیه) | کسر امتیازهای صفر |
|---|---|---|---|---|
| HI | ۱٬۰۵۰ | ۱٬۰۴۱ | **۱۵٬۶۶۵٫۷۸** | ۰٫۹۸۰ |
| LO | ۱٬۰۵۰ | ۹۹۹ | **۰٫۰۰** | ۰٫۹۸۹ |

مقدار `q_conf = 0` برای LO یک **پیامد مشروع** است، نه یک اشکال: امتیاز عدم‌انطباق یک‌طرفه
به‌صورت `s = max(0, y − Q_base)` است، بنابراین کالیبراسیون تنها می‌تواند یک چندک خام که از قبل
بیش از حد پوشش می‌دهد را *حفظ یا افزایش* دهد — هرگز نمی‌تواند آن را کوچک کند. در δ=۰٫۰۵، مقدار
Q95 خام برای LO از قبل هدف ۹۵٪ را روی مجموعه‌ی کالیبراسیون برآورده می‌کند، بنابراین نیازی به
اصلاح نیست. (اشتقاق کامل: `models/phase2/calibration/conformal_state.json`،
`conformal_config.json`.)

### ۱.۳ پوشش و تورم، پیش و پس از کالیبراسیون

| مجموعه‌ی ارزیابی | بحرانیت | پوشش خام | پوشش کالیبره‌شده | هدف | میانگین تورم (میکروثانیه) |
|---|---|---|---|---|---|
| test_id | HI | ۰٫۹۹۴۳ | **۱٫۰۰۰۰** | ۰٫۹۹ | ۱۵٬۶۶۵٫۷۸ |
| test_id | LO | ۰٫۹۸۵۷ | ۰٫۹۸۵۷ | ۰٫۹۵ | ۰٫۰۰ |
| test_ood | HI | ۰٫۹۵۶۲ | **۰٫۹۷۹۰** | ۰٫۹۹ | ۱۵٬۶۶۵٫۷۸ |
| test_ood | LO | ۰٫۹۵۱۴ | ۰٫۹۵۱۴ | ۰٫۹۵ | ۰٫۰۰ |

*(جدول کامل: `results/phase2/calibration/coverage_by_criticality.csv`،
`inflation_by_criticality.csv`؛ نمودار: `results/phase2/plots/coverage_before_after_calibration.png`،
`reliability_diagram.png`)*

نکته درباره‌ی «میانگین تورم» در بالا: از آنجا که `q_conf` یک اصلاح ثابت واحد است که به هر رکورد
درون یک سطح بحرانیت افزوده می‌شود، تورم اعمال‌شده یک **مقدار ثابت** است (۱۵٬۶۶۵٫۷۸ میکروثانیه
برای هر رکورد HI، صفر برای هر رکورد LO — یعنی یک توزیع با پراکندگی نیست). کمیتی که واقعاً توزیع
دارد، و همان چیزی که راهنمای تحویل صراحتاً گزارش آن را می‌خواهد («توزیع امتیازهای عدم‌انطباق،
شامل کسر امتیازهای صفر»)، **امتیاز عدم‌انطباق مجموعه‌ی کالیبراسیون** یعنی
`s_j = max(0, y_j − Q_base(x_j))` است، *پیش از* آنکه آماره‌ی مرتبه‌ی k_h-ام آن را به همان مقدار
واحد `q_conf` فروبکاهد — که در `results/phase2/plots/inflation_distribution.png` نمایش داده
شده (هیستوگرام با مقیاس لگاریتمی، زیرا ۹۸٫۰٪ از امتیازهای کالیبراسیون HI و ۹۸٫۹٪ از LO دقیقاً
صفر هستند، با آستانه‌ی `q_conf` نتیجه‌شده مشخص‌شده روی نمودار) در کنار یک نمودار میله‌ای از خودِ
اصلاح ثابت نتیجه‌شده.

خوانش این نتایج: روی داده‌ی درون‌توزیعی، سر خام از پیش نزدیک به مقدار اسمی است و کالیبراسیون
عمدتاً آن را تأیید می‌کند (HI حتی پس از یک اصلاح متوسط ۱۵٬۶۶۶+ میکروثانیه‌ای به پوشش تجربی کامل
می‌رسد). تحت تغییر توزیع (test_ood، گراف‌های ۵۰۱ تا ۱۰۰۰ گره‌ای که مدل هرگز روی آن‌ها آموزش
ندیده)، پوشش خام HI به ۰٫۹۵۶ افت می‌کند — کمتر از هدف ۰٫۹۹ آن — و اصلاح کالیبراسیون **منجمد و
از‌پیش‌برازش‌شده** آن را تا ۰٫۹۷۹ بازمی‌گرداند؛ هنوز کمتر از هدف اما یک بازیابی واقعی و
قابل‌سنجش صرفاً از طریق اصلاح داده‌محور بدون هیچ بازآموزشی. پوشش LO روی test_ood (۰٫۹۵۱) از قبل
هدف ۰٫۹۵ خود را به‌صورت خام برآورده می‌کند، پس — به‌درستی — در آنجا نیز هیچ اصلاحی اعمال نمی‌شود؛
این روش عددی را که خراب نبوده به‌طور مصنوعی «تعمیر» نمی‌کند.

### ۱.۴ پویش حساسیت α/δ

پویش روی `base_quantile ∈ {Q90, Q95, Q99}` × `δ ∈ {0.01, 0.05, 0.10}` انجام شد (به‌صورت
متقارن HI=LO برای این پویش؛ پیش‌فرض نامتقارن بالا همان *نقطه‌ی عملیاتی* انتخاب‌شده است). جدول
کامل: `results/phase2/calibration/calibration_sensitivity_sweep.csv`. الگوی اصلی: در
`δ=0.01`، مقدار `q_conf` دقیقاً مثبت است و با افزایش چندک پایه به‌صورت یکنوا کوچک می‌شود
(Q90→۳۴٬۴۶۹ میکروثانیه، Q95→۱۵٬۶۶۶/۲٬۴۵۷ میکروثانیه (HI/LO)، Q99→۰ میکروثانیه) — دقیقاً همان
رفتار مورد انتظار در روش هم‌ریخت، چرا که یک چندک پایه‌ی خام بالاتر برای رسیدن به همان هدف به
اصلاح کمتری نیاز دارد. در `δ≥0.05`، بیشتر خانه‌ها از قبل به `q_conf=0` فرومی‌ریزند، که تأیید
می‌کند مشاهده‌ی امتیاز صفر در بخش ۱.۲ مصنوع یک پیکربندی خاص نیست.

---

## ۲. پایداری پیش‌بینی و هندسه‌ی چندک‌ها
*(بخش مشخصات: «تحلیل پایداری و هندسه‌ی پیش‌بینی»)*

- **نرخ تقاطع چندک‌ها: ۰٫۰ در همه‌جا** (آموزش، test_id، test_ood، تمام سناریوهای
  مقیاس‌پذیری/بار دنباله‌ای) — `OrderedQuantileHead` مدل منجمد رابطه‌ی `Q50 ≤ Q90 ≤ Q95 ≤ Q99`
  را از طریق **ساخت معماری** (افزایش‌های مثبت softplus روی Q50) تحمیل می‌کند، نه از طریق یک
  جریمه‌ی نرم در آموزش. این تضمینی *قوی‌تر* از رویکرد اختیاری ترم جریمه‌ی `L_nc` مشخصات (رابطه‌ی
  ۲۸) است: مسیر معماری تقاطع را از نظر ساختاری غیرممکن می‌سازد نه صرفاً در حد انتظار نامطلوب —
  دقیقاً همین دلیل آن است که نرخ تقاطع مشاهده‌شده در این گزارش در هر مجموعه و هر سناریوی فشار
  دقیقاً ۰٫۰ است، بدون آنکه ترم جداگانه‌ی `L_nc` نیاز یا اعمال شود.
- **شکل بصری توزیع** (`results/phase2/plots/violin_predicted_quantiles_by_role.png`،
  `violin_predicted_by_hardware_scenario.png`، `box_actual_vs_predicted.png`): پراکندگی چندکی
  (Q99−Q50) به‌طور نظام‌مند بر اساس نقش گره‌ی هدف تغییر می‌کند — گره‌های `source_like`
  فشرده‌ترین و بهترین‌کالیبره‌شده‌ترین توزیع‌ها را دارند (coverage_Q95 = ۰٫۹۹۱ روی test_ood،
  میانگین pinball برابر ۱٬۲۳۱ میکروثانیه)، درحالی‌که گره‌های `sink_like` و `communication_heavy`
  هم بیشترین پراکندگی و هم بدترین pinball را دارند (به‌ترتیب ۷٬۹۱۰ و ۶٬۴۴۳ میکروثانیه) — نگاه
  کنید به بخش ۴.۴. این توزیع همچنین بر اساس سناریوی سخت‌افزاری (core_type × سطح DVFS) به‌طور
  محسوسی جابه‌جا می‌شود: سناریوهای هسته‌ی `little` با DVFS پایین، هم میانه‌ی بالاتر و هم پراکندگی
  Q95 گسترده‌تری نسبت به سناریوهای هسته‌ی `big` با DVFS بالا نشان می‌دهند، که مستقیماً حساسیت
  هسته/DVFS کمّی‌شده در بخش ۳.۱ را بازتاب می‌دهد.
- **روش‌های مبنای برآورد نقطه‌ای اصلاً نمی‌توانند این را بیان کنند.** روش‌های مبنای
  MLP/GNN-Mean در بخش ۵ فقط یک عدد به ازای هر (گره، زمینه) تولید می‌کنند؛ آن‌ها نمی‌توانند
  علامت‌گذاری کنند که «زمان اجرای این گره در حال حاضر به‌طور غیرمعمول نامطمئن است»، که دقیقاً
  همان اطلاعاتی است که یک زمان‌بند برای تصمیم‌گیری درباره‌ی پذیرش یک کار با بحرانیت HI به آن نیاز
  دارد. این ارزش عملی و ملموس نمایش چندکی نسبت به یک مدل صرفاً میانگین‌محور است، فراتر از شکاف
  MAE کلی در بخش ۵.

---

## ۳. مقیاس‌پذیری و ناهمگنی سامانه
*(بخش مشخصات: «ارزیابی مقیاس‌پذیری و ناهمگنی سامانه»)*

### ۳.۱ حساسیت core_type / DVFS (داده‌ی آزمون موجود، حقیقت پایه‌ی واقعی)

| core_type | میانگین pinball (میکروثانیه) | MAE (میکروثانیه) | coverage_Q95 |
|---|---|---|---|
| big | ۳٬۵۳۶ | ۱۲٬۷۷۲ | ۰٫۹۶۰ |
| little | ۵٬۵۱۱ | ۲۰٬۵۲۸ | ۰٫۹۴۸ |

| dvfs_level | میانگین pinball (میکروثانیه) | MAE (میکروثانیه) | coverage_Q95 |
|---|---|---|---|
| ۰ (کمترین فرکانس) | ۶٬۰۸۵ | ۲۳٬۳۵۰ | ۰٫۹۴۲ |
| ۱ | ۴٬۵۶۸ | ۱۶٬۴۸۱ | ۰٫۹۶۶ |
| ۲ | ۳٬۷۳۸ | ۱۳٬۸۳۹ | ۰٫۹۵۸ |
| ۳ (بیشترین فرکانس) | ۳٬۷۱۹ | ۱۲٬۹۹۳ | ۰٫۹۴۹ |

*(test_ood؛ جداول کامل `results/phase2/ood/grouped_ood_metrics_by_{core_type,dvfs_level}.csv`؛
نمودار `results/phase2/plots/dvfs_sensitivity.png`)* هر دو الگو از نظر جهت منطقی هستند:
هسته‌های `little` مدت زمان بیشتری اجرا می‌شوند و زمان‌های اجرای (نسبتاً بزرگ‌تر) آن‌ها پیش‌بینی
دقیق آن‌ها بر حسب میکروثانیه‌ی مطلق سخت‌تر است؛ پایین‌ترین سطح DVFS (کندترین، طولانی‌ترین اجرا)
سخت‌ترین نقطه‌ی عملیاتی برای پیش‌بینی دقیق است، که با مقیاس‌گیری خطا متناسب با بزرگی مقدار
پیش‌بینی‌شده سازگار است. این شاهدی مستقیم بر این است که مدل رفتاری واقعاً متفاوت و غیرپیش‌پاافتاده
به ازای هر ترکیب هسته/DVFS آموخته است، نه یک میانگین سراسری واحد.

### ۳.۲ رفتار گره‌های محاسبات‌محور در برابر حافظه‌محور

| operation_type | میانگین pinball در test_id (میکروثانیه) | میانگین pinball در test_ood (میکروثانیه) | coverage_Q95 در test_ood |
|---|---|---|---|
| compute_bound | ۲٬۶۲۹ | **۵٬۸۶۸** | ۰٫۹۳۰ |
| balanced | ۲٬۴۹۴ | ۵٬۲۶۰ | ۰٫۹۴۶ |
| memory_bound | ۲٬۶۲۳ | **۲٬۵۴۷** | ۰٫۹۸۳ |

*(`results/phase2/ood/grouped_{test_id,test_ood}_metrics_by_operation_type.csv`)* روی
داده‌ی درون‌توزیعی، هر سه نوع عملیات با دقتی قابل‌مقایسه پیش‌بینی می‌شوند
(۲٬۴۹۴ تا ۲٬۶۲۹ میکروثانیه pinball) — شاهدی بر اینکه مدل یاد گرفته هر رژیم را مدیریت کند، نه
صرفاً یک الگوی غالب. تحت تغییر توزیع، این سه نوع **به‌شدت واگرا می‌شوند**: گره‌های حافظه‌محور به
سختی افت می‌کنند (۲٬۶۲۳→۲٬۵۴۷ میکروثانیه، اساساً بدون تغییر) درحالی‌که گره‌های محاسبات‌محور بیش
از ۲ برابر افت می‌کنند (۲٬۶۲۹→۵٬۸۶۸ میکروثانیه). این یک الگوی تعمیم‌پذیری واقعاً غیرخطی و
وابسته به رژیم است — چیزی که یک ضریب مقیاس‌گیری سراسری واحد نمی‌تواند تولید کند — و شاهدی مستقیم
است که مدل رفتار درونی کیفیتاً متفاوتی برای گره‌های محاسبات‌محور در برابر حافظه‌محور آموخته است،
دقیقاً همان چیزی که عبارت‌بندی مشخصات خواستار اثبات آن است.

### ۳.۳ آزمون فشار مقیاس‌پذیری ۴/۸/۱۶/۳۲ هسته‌ای

**چارچوب‌بندی (صراحتاً بیان‌شده چون تفسیر درست را تغییر می‌دهد):** `core_id` هرگز یک ورودی مدل
فاز ۱ نبوده (فقط `core_type` ∈ {big, little})؛ مدل هیچ مفهومی از *چند تا* هسته وجود دارد ندارد.
یک بازشبیه‌سازی واقعی سخت‌افزار ۳۲ هسته‌ای چیزی نیست که این پیش‌بین منجمد بتواند تولید کند. آنچه
*می‌توان* صادقانه آزمود، تنها ویژگی‌ای است که فشار تعداد هسته را رمزگذاری می‌کند —
`active_core_count` — که از بازه‌ی آموزشی خود `[۱، ۴]` به ۴، ۸، ۱۶ و ۳۲ روی زمینه‌های واقعی
test_id هل داده شده است (توپولوژی، core_type، DVFS و هر ویژگی دیگر z_t ثابت نگه داشته شده‌اند).
هیچ حقیقت پایه‌ای برای نسخه‌ی فرضی ۳۲ هسته‌ای این سامانه‌ی مصنوعی وجود ندارد، بنابراین این
نتیجه صرفاً به‌عنوان یک **مشخصه‌سازی عدم‌قطعیت/استحکام پیش‌بینی** گزارش می‌شود، هرگز به‌عنوان یک
ادعای دقت.

| سناریو | active_core_count | امتیاز-z (برازش‌شده روی داده‌ی آموزش) | میانگین Q50 (میکروثانیه) | میانگین C_e (میکروثانیه) | میانگین پراکندگی Q99−Q50 (میکروثانیه) |
|---|---|---|---|---|---|
| core_4 | ۴ | ۱٫۸۳ | ۲۵۲٬۸۶۶ | ۲۹۲٬۷۵۵ | ۴۷٬۸۳۳ |
| core_8 | ۸ | ۶٫۷۴ | ۲۵۲٬۰۳۷ | ۲۹۱٬۷۶۹ | ۴۷٬۶۰۸ |
| core_16 | ۱۶ | ۱۶٫۵۴ | ۲۵۰٬۶۷۵ | ۲۹۰٬۱۰۱ | ۴۷٬۱۶۶ |
| core_32 | ۳۲ | ۳۶٫۱۵ | ۲۴۵٬۵۹۰ | ۲۸۴٬۱۲۴ | ۴۵٬۸۹۴ |

*(`results/phase2/scalability/core_scalability_summary.csv`؛ نمودار
`results/phase2/plots/scalability_core_count.png`)*

**یافته‌ی مرتبط با ایمنی:** زمان اجرای پیش‌بینی‌شده و بودجه‌ی کالیبره‌شده هر دو با هل‌داده‌شدن
فزاینده‌ی `active_core_count` به بیرون از بازه‌ی آموزشی (امتیاز-z تا ۳۶ انحراف معیار) *اندکی افت
می‌کنند*، به‌جای افزایش همان‌طور که یک شهود ساده (تراکم بیشتر ← زمان اجرای طولانی‌تر) انتظار
دارد. هم پراکندگی چندکی و هم بودجه‌ی کالیبره‌شده با هم کوچک می‌شوند — مدل *مطمئن‌تر* می‌شود، نه
کمتر، دقیقاً در جایی که باید *کمترین* اعتماد به آن باشد. این دقیقاً همان نوع **برون‌یابی
غیرمحافظه‌کارانه** است که ادبیات OOD نسبت به آن هشدار می‌دهد و یک استقرار واقعی باید در برابر آن
محافظت کند (برای مثال، با امتناع از اعتماد به پیش‌بینی‌ها هرگاه امتیاز-z استانداردشده‌ی یک ویژگی
از یک آستانه‌ی ثابت فراتر رود، یا با گسترش صریح `δ_h` هرگاه `active_core_count > 4` در زمان
استنتاج تشخیص داده شود).

### ۳.۴ توازن DVFS / انرژی / ایمنی (کیفی)

انرژی نسبی ∝ V²·f به ازای هر سیکل فعال (نه توان کالیبره‌شده، فقط برای توازن کیفی استفاده
می‌شود): رفتن از سطح DVFS صفر به ۳ روی هسته‌های `big` تغییری از ۱٫۰ به ۲٫۲ گیگاهرتز، ۰٫۹۰ به
۱٫۱۵ ولت است، یعنی تقریباً یک افزایش انرژی نسبی به ازای هر سیکل به میزان **۲٫۵۵ برابر** برای
زمان اجرایی که (طبق بخش ۳.۱) کاهش می‌یابد و پیش‌بینی دقیق آن به‌طور محسوسی آسان‌تر می‌شود
(pinball از ۶٬۰۸۵ به ۳٬۷۱۹ میکروثانیه). توازنی که بودجه‌های این پروژه در اختیار یک زمان‌بند قرار
می‌دهند صریح است: سطوح پایین‌تر DVFS به ازای هر سیکل ارزان‌ترند اما بودجه‌های زمان اجرایی هم
طولانی‌تر و هم از نظر آماری کمتر قابل‌اتکا تولید می‌کنند — یک توازن واقعی انرژی/ایمنی/موعد زمانی
که یک زمان‌بند آگاه از DVFS در لایه‌های پایین‌دستی می‌تواند از آن بهره ببرد (برای مثال، رزرو
پایین‌ترین سطوح DVFS تنها برای کارهای با بحرانیت LO و دارای اسلک فراوان).

---

## ۴. ساختار گراف و مقیاس‌پذیری
*(بخش مشخصات: «تحلیل ساختار و مقیاس‌پذیری گراف»)*

### ۴.۱ مقیاس‌گیری تعداد گره (۵۰ تا ۱۰۰۰ گره)

| مجموعه‌ی ارزیابی | n | میانگین pinball (میکروثانیه) | MAE (میکروثانیه) | coverage_Q95 |
|---|---|---|---|---|
| test_id (۵۰ تا ۵۰۰ گره، بازه‌ی آموزش) | ۲٬۱۰۰ | ۲٬۵۸۴٫۵ | ۱۰٬۹۰۷٫۸ | ۰٫۹۹۰ |
| test_ood (۵۰۱ تا ۱۰۰۰ گره) | ۲٬۱۰۰ | ۴٬۵۲۳٫۸ | ۱۶٬۶۵۰٫۰ | ۰٫۹۵۴ |

مقدار `num_nodes` به‌طور **۱۰۰٪** به‌واسطه‌ی نحوه‌ی ساخت، در test_ood بیرون از بازه‌ی آموزشی
`[۵۱، ۴۹۷]` است؛ `depth`/`max_width` نیز حتی *درون* مجموعه‌ی ۵۰۱ تا ۱۰۰۰ گره‌ای حدود ۱۰٪ بیرون
از بازه هستند. دقت به میزان تقریباً ۱٫۷۵ برابر (pinball) افت می‌کند اما فرونمی‌پاشد — مدل همچنان
چندک‌های قابل‌استفاده، مرتب و بدون تقاطع را روی گراف‌هایی دو برابر بزرگ‌تر از هر آنچه روی آن
آموزش دیده تولید می‌کند. (`results/phase2/ood/structural_feature_range_report.csv`،
`accuracy_vs_graph_size.png`)

**زمان آموزش در برابر اندازه‌ی گراف در اینجا از قصد قابل‌اعمال نیست** — پیش‌بین فاز ۱ منجمد است
و در فاز ۲ به ازای هیچ اندازه‌ی گرافی دوباره آموزش داده نمی‌شود (بازآموزی قاعده‌ی اصلی بخش ۰ را
نقض می‌کند). در عوض **زمان استنتاج** به‌طور مستقیم اندازه‌گیری شد (یک پاس رو به جلوی تکی،
`_infer_one_graph`، زمان دیواری، روی CPU):

| مجموعه‌ی ارزیابی | میانگین زمان استنتاج (میلی‌ثانیه) | میانه | بیشینه |
|---|---|---|---|
| test_id (۵۰ تا ۵۰۰ گره) | ۲۴٫۲ | ۱۱٫۶ | ۱۲۸٫۵ |
| test_ood (۵۰۱ تا ۱۰۰۰ گره) | **۱۱۲٫۵** | ۵۳٫۰ | ۷۷۹٫۴ |

همبستگی پیرسون بین `num_nodes` و `inference_time_ms` = **۰٫۴۴** در هر دو مجموعه — یک رابطه‌ی
مثبت متوسط، سازگار با هزینه‌ی پیام‌رسانی به ازای هر گره در رمزگذار DAG، نه یک انفجار آسیب‌زا.
گراف‌های test_ood (به‌طور میانگین ۲ برابر تعداد گره) حدود ۴٫۶ برابر بیشتر طول می‌کشند — که
همچنان کاملاً درون بودجه‌های نزدیک به بی‌درنگ (بدترین حالت مشاهده‌شده: ۷۷۹ میلی‌ثانیه برای
بزرگ‌ترین گراف تکی) برای یک کاربرد زمان‌بندی آفلاین یا نزدیک به آفلاین است، هرچند برای پرسمان
مجدد آنلاین به ازای هر زمینه در هر تیک زمان‌بندی روی بزرگ‌ترین گراف‌ها آشکارا کند است.
(`results/phase2/ood/inference_time_vs_graph_size.csv`)

### ۴.۲ حساسیت به «شکل» ساختاری (fat / density_parameter / regular، بازه‌ی مولد ۰٫۲ تا ۰٫۸)

| بُعد | pinball در پایین (۰٫۲) | pinball در میانه (۰٫۴ تا ۰٫۶) | pinball در بالا (۰٫۸) |
|---|---|---|---|
| fat (نسبت عرض به عمق) | ۴٬۶۳۳ | **۲٬۶۸۴** | ۵٬۴۸۲ |
| density_parameter | **۲٬۳۱۳** | ۳٬۷۰۰ | ۵٬۸۱۸ |
| regular (نظم) | ۶٬۸۲۴ | ۵٬۲۱۴ | **۳٬۲۵۴** |

*(test_ood؛ `results/phase2/ood/grouped_ood_metrics_by_{fat,density_parameter,regular}_bin.csv`)*
چگالی قوی‌ترین عامل است: گراف‌های پراکنده (چگالی پایین) به‌طور محسوسی بهتر از گراف‌های چگال
پیش‌بینی می‌شوند (۲٬۳۱۳ در برابر ۵٬۸۱۸ میکروثانیه pinball) — که احتمالاً به این دلیل است که
گراف‌های چگال تداخل پیام‌رسانی بیشتری به ازای هر گره و ترم‌های حجم ورودی بزرگ‌تر و
دشوارتر-برای-تفکیک ایجاد می‌کنند. گراف‌های بسیار منظم آسان‌تر از گراف‌های نامنظم هستند (۳٬۲۵۴
در برابر ۶٬۸۲۴ میکروثانیه)، که با این ایده سازگار است که همسایگی‌های محلی همگن‌تر برای خلاصه‌سازی
توسط لایه‌های توجه آسان‌تر هستند.

### ۴.۳ همبستگی مسیر بحرانی — هم پروکسی توپولوژیک و هم نمایش نهفته‌ی آموخته‌شده‌ی GNN

پروکسی عضویت در مسیر بحرانی: `topo_level(v) + reverse_topo_level(v) == depth(G) − 1`
(یک پروکسی توپولوژیک و مبتنی بر شمارش گام برای طولانی‌ترین مسیر — نه وزن‌دار بر اساس
compute_cycles؛ به‌عنوان یک محدودیت در بخش ۸ بیان شده است).

| مجموعه‌ی ارزیابی | is_critical_path_node | coverage_Q95 |
|---|---|---|
| test_ood | False | **۰٫۹۷۷** |
| test_ood | True | ۰٫۹۴۶ |

*(`results/phase2/ood/critical_path_accuracy_comparison.csv`، `critical_path_coverage.png`)*
گره‌های روی مسیر بحرانی به‌طور قابل‌سنجشی نسبت به سایر گره‌ها تحت تغییر توزیع پوشش کمتری دارند —
یافته‌ای مستقیماً مرتبط با زمان‌بندی، از آنجا که این‌ها دقیقاً همان گره‌هایی هستند که خطای آن‌ها
بیشترین تأثیر را بر زمان تکمیل سرتاسری یک DAG می‌گذارد.

فراتر از پروکسی توپولوژیک، مشخصات صراحتاً همبستگی بین نمایش **آموخته‌شده‌ی** GNN و دقت را
درخواست می‌کند. `model.dag_encoder(x, edge_index, edge_attr)` به‌صورت فقط-خواندنی (بدون
به‌روزرسانی وزن) فراخوانی شد تا نمایش نهفته‌ی پنهان واقعی هر گره‌ی هدف استخراج شود، و دو ویژگی
مشتق‌شده با خطای هر رکورد همبسته شدند (`src/phase2/embeddings.py`،
`scripts/run_phase2_deepdive.py`):

| همبستگی با... | pinball@Q95 | خطای مطلق @Q50 |
|---|---|---|
| `embedding_norm` (test_id) | −۰٫۳۵۳ | −۰٫۳۳۱ |
| `embedding_dist_from_graph_mean` (test_id) | +۰٫۲۵۶ | +۰٫۲۰۴ |
| `embedding_norm` (test_ood) | −۰٫۱۲۹ | −۰٫۲۶۷ |
| `embedding_dist_from_graph_mean` (test_ood) | +۰٫۱۴۳ | +۰٫۱۸۶ |

*(`results/phase2/ood/embedding_error_correlation_{test_id,test_ood}.csv`)* دو سیگنال
سازگار و قابل‌تفسیر: (۱) نُرم‌های بزرگ‌تر نمایش نهفته با خطای *کمتر* همبسته‌اند — گره‌هایی که
رمزگذار آن‌ها را با فعال‌سازی قوی‌تر/مطمئن‌تر نمایش می‌دهد دقیق‌تر پیش‌بینی می‌شوند؛ (۲) نمایش‌های
نهفته‌ای که *دورتر از میانگین نمایش نهفته‌ی گراف خودشان* قرار دارند — یعنی گره‌های ساختاری غیرمعمول
نسبت به DAG خود — در هر دو مجموعه با خطای *بیشتر* همبسته‌اند. این به‌طور مستقل یافته‌ی توپولوژیک
بالا را تأیید می‌کند: روی test_ood، گره‌های مسیر بحرانی (طبق پروکسی توپولوژیک) هم میانگین pinball
بالاتری دارند (۳٬۸۰۰ در برابر ۲٬۱۴۶ میکروثانیه) و هم میانگین `embedding_dist_from_graph_mean`
بالاتری (۶٫۳۶ در برابر ۵٫۹۰) نسبت به گره‌های غیرمسیر-بحرانی
(`results/phase2/ood/embedding_by_critical_path_test_ood.csv`) — دو سنجه‌ی مستقل از «این گره
چقدر دشوار است» با یکدیگر هم‌رأی هستند.

### ۴.۴ تأثیر ویژگی یالی / حجم ارتباطی

| target_role | میانگین pinball (میکروثانیه) | coverage_Q95 |
|---|---|---|
| source_like | **۱٬۲۳۱** | ۰٫۹۹۱ |
| high_degree | ۳٬۰۱۹ | ۰٫۹۶۷ |
| random | ۳٬۱۶۶ | ۰٫۹۸۹ |
| diverse_fill_2 | ۲٬۰۳۶ | ۱٫۰۰۰ |
| diverse_fill_1 | ۴٬۱۲۶ | ۰٫۹۴۱ |
| middle | ۵٬۵۷۵ | ۰٫۹۳۱ |
| communication_heavy | ۶٬۴۴۳ | **۰٫۹۰۰** |
| sink_like | **۷٬۹۱۰** | ۰٫۹۴۰ |

*(test_ood؛ `results/phase2/ood/communication_role_accuracy.csv`)* گره‌هایی که صراحتاً به‌عنوان
`communication_heavy` انتخاب شده‌اند (حجم ورودی بزرگ `input_size_bytes` / `data_bytes`) هم
بدترین پوشش را نشان می‌دهند (۰٫۹۰۰، به‌وضوح کمتر از هدف ۰٫۹۵) و هم در میان بدترین pinball هر
نقشی هستند — شاهدی مستقیم بر اینکه ویژگی‌های یالی/ارتباطی منبعی واقعی از دشواری باقیمانده هستند
که مدل تحت تغییر توزیع هنوز به‌طور کامل بر آن مسلط نشده، و استدلالی قوی برای رفتار محافظه‌کارانه‌تر
با این گره‌ها (حاشیه‌ی `δ` بالاتر) در یک استقرار آگاه از بحرانیت.

---

## ۵. مقایسه‌ی روش‌های مبنا
*(بخش مشخصات: «مقایسه خروجی مدل با روش‌هایی که تنها میانگین را پیش‌بینی می‌کنند»؛ کد:
`src/phase2/baselines.py`، `scripts/run_phase2_baselines.py`)*

دو مدل مقایسه‌ای جدید و مستقل آموزش‌دیده (که هرگز پیش‌بین منجمد را لمس نمی‌کنند)، تحت یک بودجه‌ی
محاسباتی صراحتاً کاهش‌یافته (۱۵۰/۳۰ گراف آموزش/اعتبارسنجی، ۸ دوره، CPU) برای امکان‌پذیری آموزش
داده شدند — که هدف آن‌ها ایجاد مقایسه‌های جهت‌دار است، نه رقبایی که به‌طور کامل تنظیم شده باشند:

| مدل | MAE (میکروثانیه) | RMSE (میکروثانیه) | چه چیزی را حذف می‌کند |
|---|---|---|---|
| MLP (بدون گراف) | ۶۸٬۶۹۷ | ۹۹٬۰۶۰ | بدون پیام‌رسانی — فقط ویژگی‌های ایستای خود گره‌ی هدف + زمینه |
| GNN-Mean | ۵۰٬۴۶۴ | ۷۵٬۲۲۱ | همان خانواده‌ی DAGEncoder/ContextFusionEncoder، سر میانگین با MSE به‌جای سر چندکی با pinball |
| GNN-Mean-NoZt (آفلاین) | ۴۸٬۲۴۸ | ۷۱٬۹۶۱ | ویژگی‌های z_t صفر شده‌اند (میانگین استانداردشده) — زمان‌بند «آفلاین» ناآگاه از وضعیت جاری سامانه |
| **GNN-Quantile (منجمد، Q50)** | **۱۰٬۹۰۸** | **۱۷٬۴۶۲** | *(پیش‌بین واقعی فاز ۱ — بودجه‌ی کامل آموزش، زیان چندکی)* |

*(`results/phase2/baselines/model_comparison.csv`، `plots/model_comparison_mae.png`)*

**MLP ← GNN-Mean:** افزودن ساختار گراف (پیام‌رسانی روی پیشین‌ها/پسین‌ها) حتی تحت این بودجه‌ی
آموزش کاهش‌یافته MAE را حدود ۲۷٪ کاهش می‌دهد — شاهدی مستقیم بر اینکه توپولوژی DAG سیگنال
پیش‌بینی واقعی فراتر از ویژگی‌های خود یک گره حمل می‌کند.

**GNN-Mean ← GNN-Quantile منجمد:** MAE مدل منجمد **حدود ۴٫۶ برابر کمتر** از GNN-Mean با بودجه‌ی
کاهش‌یافته است. با توجه به اینکه هر دو خانواده‌ی معماری پایه‌ی یکسانی را به اشتراک می‌گذارند، این
شکاف به (الف) بودجه‌ی کامل آموزش (۱۰۳ دوره / توقف زودهنگام در برابر ۸ دوره) و (ب) خودِ هدف آموزشی
چندکی/pinball نسبت داده می‌شود، که — برخلاف MSE ساده — صراحتاً هر دو دنباله را جریمه می‌کند و
به‌طور طبیعی به‌سمت میانه‌ی شرطی به‌جای میانگینی که می‌تواند توسط توزیع راست‌چوله‌ی زمان اجرا کشیده
شود، منظم‌سازی می‌کند.

**آنلاینِ آگاه از z_t در برابر آفلاینِ ناآگاه از z_t:** تحت این بودجه‌ی آموزش کاهش‌یافته، آزمایش
حذفی GNN-Mean-NoZt به‌طور قطعی *بدتر* از GNN-Mean نیست (MAE برابر ۴۸٬۲۴۸ در برابر ۵۰٬۴۶۴
میکروثانیه — درون باند نویز دو اجرای مستقل‌مقداردهی‌اولیه‌شده و سبک‌آموزش‌دیده)، بنابراین این
آزمایش حذفی خاص **در این بودجه‌ی محاسباتی نتیجه‌گیری‌ناپذیر** است و صادقانه همین‌گونه گزارش
می‌شود، به‌جای تحمیل یک روایت. تشخیص‌های *خودِ* مدل منجمد در این پرسش قاطع‌ترند: معیارهای
گروه‌بندی‌شده‌ی فاز ۱ بر اساس `cpu_utilization`/`bus_utilization`
(`results/phase1/metrics/test_id_grouped_{cpu_utilization,bus_utilization}.csv`) نشان می‌دهند
که میانگین زیان pinball به‌طور نظام‌مند و یکنوا با صندوقچه‌ی بهره‌برداری افزایش می‌یابد — از
۲٬۱۳۹ به ۳٬۰۸۹ میکروثانیه در صندوقچه‌های بهره‌برداری CPU، از ۲٬۲۱۶ به ۳٬۲۱۸ میکروثانیه در
صندوقچه‌های بهره‌برداری گذرگاه روی test-ID — چیزی که اگر خروجی مدل منجمد نسبت به `z_t` بی‌تفاوت
بود رخ نمی‌داد. در مجموع، شواهد این است: مدل منجمد و کاملاً آموزش‌دیده به‌طور قابل‌اثبات نسبت به
`z_t` حساس **است**؛ اینکه آیا یک آزمایش حذفی با بودجه‌ی کاهش‌یافته می‌تواند آن حساسیت را در چند
دقیقه آموزش روی CPU به‌طور تمیز جدا کند، یک نتیجه‌ی (منفی) جداگانه درباره‌ی بودجه‌ی محاسباتی
روش‌های مبنا است، نه درباره‌ی پیش‌بین منجمد.

---

## ۶. تعمیم‌پذیری و تغییر توزیع
*(بخش مشخصات: «آزمون تعمیم‌پذیری و تغییر توزیع»)*

### ۶.۱ گراف‌های بزرگ‌تر — در بخش ۴.۱ پوشش داده شده است (test_ood).

### ۶.۲ استحکام در برابر تغییرات ناگهانی z_t
*(کد: `src/phase2/drift.py`، `scripts/run_phase2_drift.py`)*

دو پرسش واقعاً متفاوت، طبق هشدار راهنمای تحویل درباره‌ی درهم‌آمیختن آن‌ها، جدا نگه داشته شده‌اند:

**(الف) فشار بار دنباله‌ای درون‌بازه‌ی واقعی** (حقیقت پایه‌ی معتبر — دهک بالای یک پروکسی بار
ترکیبی حافظه/گذرگاه/حرارتی/jitter روی رکوردهای واقعی test_ood):

| is_high_load_tail | n | میانگین pinball (میکروثانیه) | MAE (میکروثانیه) | coverage_Q95 |
|---|---|---|---|---|
| False | ۱٬۸۹۰ | ۴٬۳۹۲ | ۱۶٬۲۱۷ | ۰٫۹۵۶ |
| **True** | ۲۱۰ | **۵٬۷۱۰** | **۲۰٬۵۴۹** | **۰٫۹۳۳** |

افت دقت واقعی و مبتنی بر حقیقت پایه تحت شرایط فشار بالای واقعی.
(`results/phase2/drift/tail_load_stress_accuracy.csv`)

**(ب) اضافه‌بار مصنوعی شدید** (صرفاً توصیفی — `memory_active_tasks=10`،
`bus_utilization=1.4`، `thermal_pressure=1.3`، `release_jitter_us=900`، همه به‌وضوح فراتر از
هر بازه‌ی آموزشی؛ هیچ حقیقت پایه‌ی ساختگی استفاده نمی‌شود):

| سناریو | میانگین Q50 (میکروثانیه) | میانگین C_e (میکروثانیه) | Δ C_e نسبت به خط پایه |
|---|---|---|---|
| baseline_test_id | ۲۵۳٬۰۰۹ | ۲۹۲٬۹۳۰ | — |
| synthetic_overload | ۴۵۷٬۵۴۱ | ۵۳۲٬۷۱۳ | **+۸۱٫۹٪** |

(`results/phase2/drift/synthetic_overload_comparison.csv`) برخلاف فشار مقیاس‌پذیری
`active_core_count` در بخش ۳.۲، مدل در اینجا **از نظر جهت به‌طور منطقی** واکنش نشان می‌دهد —
هزینه‌ی پیش‌بینی‌شده و بودجه‌ی کالیبره‌شده به‌جای افت، تحت اضافه‌بار به‌شدت افزایش می‌یابند. توضیح
محتمل: فشار حافظه/گذرگاه/حرارتی در طول آموزش به‌طور متراکم در سرتاسر بازه‌ی کامل خود نمونه‌برداری
شده‌اند (برگرفته از Beta(2.2, 2.2)، لایه‌بندی‌شده طبق بخش ۶ راهنمای تحویل)، درحالی‌که
`active_core_count` تنها در یک بازه‌ی باریک چهارنقطه‌ای مشاهده شده — فرضیه‌ای مشخص و
قابل‌آزمایش برای *چرایی* اینکه چرا یک جهت OOD به‌طور ایمن برون‌یابی می‌شود و دیگری نمی‌شود.

**(ج) کالیبراسیون ایستا در برابر پنجره‌ی لغزان (علّی، تطبیقی)** — یک نسخه‌ی آنلاین و
صرفاً-گذشته‌نگر (اندازه‌ی پنجره = ۵ DAG ≈ ۲۱۰ رکورد؛ اصلاح هر پنجره منحصراً روی باقیمانده‌های
**از‌پیش‌آشکارشده** از پنجره‌های قبلی برازش می‌شود، هرگز پنجره‌های آینده):

| بحرانیت | پوشش هدف | پوشش ایستا | پوشش پنجره‌ی لغزان | بودجه‌ی ایستا (میکروثانیه) | بودجه‌ی پنجره‌ی لغزان (میکروثانیه) |
|---|---|---|---|---|---|
| HI | ۰٫۹۹ | ۰٫۹۷۹ | **۰٫۹۸۷** | ۳۲۸٬۶۳۹ | ۳۵۵٬۵۹۰ (+۸٫۲٪) |
| LO | ۰٫۹۵ | ۰٫۹۵۱ | **۰٫۹۶۲** | ۲۹۳٬۰۵۷ | ۲۹۷٬۹۶۴ (+۱٫۷٪) |

(`results/phase2/drift/static_vs_sliding_window_calibration.csv`،
`plots/static_vs_sliding_window_calibration.png`) نسخه‌ی تطبیقی پوشش نزدیک‌تری به هدف تحت
تغییر توزیع test_ood برای هر دو سطح بحرانیت بازیابی می‌کند، به قیمت یک بودجه‌ی میانگین اندکی
(HI: +۸٫۲٪، LO: +۱٫۷٪) بزرگ‌تر — یک نمایش مشروع و از نظر علّی معتبر که کالیبراسیون آنلاین/تطبیقی
یک راهکار کاهش‌دهنده‌ی عملی برای بار دنباله‌ای است، که مستقیماً به درخواست مشخصات برای مقایسه‌ی
کالیبراسیون ایستا با پنجره‌ی لغزان پاسخ می‌دهد.

### ۶.۳ نرخ فراروی/تحریک تعویض‌حالت در بحرانیت آمیخته

یک شرط استاندارد تحریک در بحرانیت آمیخته این است: *یک کار با بحرانیت HI از بودجه‌ی خوش‌بینانه‌ی
(سطح LO) خود فراتر می‌رود*. با استفاده از چندک خام HI منجمد به‌علاوه‌ی اصلاح هم‌ریخت **LO**
(`q_conf^(LO)=0`) به‌عنوان بودجه‌ی فرضی حالت-LO برای کارهای HI:

| مجموعه‌ی ارزیابی | n (HI) | نرخ تحریک تعویض‌حالت | فراروی HI نسبت به بودجه‌ی کالیبره‌شده‌ی خودش (HI) |
|---|---|---|---|
| test_id | ۱٬۰۵۰ | ۰٫۵۷٪ | ۰٫۰۰٪ |
| test_ood | ۱٬۰۵۰ | **۴٫۳۸٪** | ۲٫۱۰٪ |

(`results/phase2/ood/mixed_criticality_mode_switch.csv`) تحت تغییر توزیع، یک کار با بحرانیت HI
باید تقریباً **۷٫۷ برابر بیشتر** (۰٫۵۷٪ ← ۴٫۳۸٪) نسبت به حالت درون‌توزیعی یک تعویض‌حالت LO←HI را
تحریک کند — شاهدی کمّی بر اینکه کالیبراسیون تفکیک‌شده بر اساس بحرانیت دقیقاً تحت شرایطی (OOD، بار
دنباله‌ای) که یک سامانه‌ی بی‌درنگ با بحرانیت آمیخته بیشترین ریسک را دارد، ارزش واقعی فراهم
می‌کند.

**تأثیر بر سرویس‌دهی به بحرانیت LO** (کیفی، از آنجا که هیچ زمان‌اجرای زنده‌ی چندحالته‌ای در اینجا
شبیه‌سازی نشده — نگاه کنید به بخش ۸): در یک سیاست استاندارد بحرانیت آمیخته، هر تعویض‌حالت LO←HI
کارهای LO هم‌زمان در حال اجرا را برای باقی‌مانده‌ی آن ابرتناوب تنزل می‌دهد یا کنار می‌گذارد. از
آنجا که آزمایش زمان‌بندی بخش ۷، ۲۵ DAG با بحرانیت HI و ۲۵ DAG با بحرانیت LO را روی همان بستر
۴-هسته‌ای کوچک‌تر از نیاز قرار می‌دهد، و بخش ۷ از قبل نشان می‌دهد DMR بحرانیت HI تحت بودجه‌ی
کالیبره‌شده برابر **۱٫۰۰** است (تمام DAGهای HI در این مجموعه‌ی آزمون)، سامانه‌ای که واقعاً
تعویض‌حالت را روی فراروی HI اعمال کند، در بدترین حالتی که این اعداد ضمنی دارند، مجبور خواهد شد
سرویس LO را روی این بستر اساساً به‌طور پیوسته تنزل دهد — این خود نشانه‌ای است از کوچک‌تر بودن بستر
نسبت به تقاضای موازی‌سازی این گراف‌ها (بخش ۷)، نه یک نقص در روش کالیبراسیون.

**ورودهای بارانی (bursty):** آزمایش زمان‌بندی در بخش ۷ هر کار DAG را به‌صورت مجزا ارزیابی می‌کند
(فرض صریح، بخش ۷/۸) و چندین *رهاسازی* DAG که نزدیک به هم در زمان می‌رسند را مدل نمی‌کند — رفتار
واقعی کنترل پذیرش برای ورود بارانی خارج از دامنه‌ی این تحویل‌شدنی است. آنچه *پوشش داده شده*،
پروکسی درون‌رکوردی برای بارانی‌بودن است که مجموعه‌داده واقعاً فراهم می‌کند، یعنی
`release_jitter_us`، که یکی از چهار ویژگی تشکیل‌دهنده‌ی آزمون فشار بار دنباله‌ای در بخش ۶.۲(الف)
است (حقیقت پایه‌ی واقعی، pinball دهک بالا ۵٬۷۱۰ در برابر ۴٬۳۹۲ میکروثانیه در جای دیگر) — بنابراین
افت ناشی از jitter رهاسازی سنجیده شده، هرچند بارانی‌بودن بین‌رهاسازی کارهای کامل DAG سنجیده
نشده است.

---

## ۷. تأثیر بر عملکرد زمان‌بندی
*(بخش مشخصات: «تأثیر بر عملکرد زمان‌بندی»؛ کد: `src/phase2/scheduling.py`،
`scripts/run_phase2_scheduling.py`)*

**فرض ساده‌سازی‌کننده‌ی بیان‌شده:** هرکدام از ۵۰ DAG مجموعه‌ی test_id به‌صورت **مجزا** روی یک
بستر ۴-هسته‌ای (۲ big + ۲ little) در غیر این صورت بی‌کار زمان‌بندی می‌شود — هیچ تداخل چندکاره/
چندمستأجره مدل نمی‌شود. این تأثیر *برآوردکننده‌ی هزینه* (که همان سهم واقعی این پروژه است) را از
سیاست کنترل پذیرش، که خارج از دامنه‌ی اینجاست، جدا می‌کند.

**پیش‌بینی هزینه‌ی کامل DAG:** ۴۲ رکورد نمونه‌برداری‌شده به ازای هر DAG تنها ۶ گره از حداکثر
۵۰۰ گره را پوشش می‌دهند؛ زمان‌بندی به هزینه‌ای برای *هر* گره نیاز دارد. از آنجا که `F_θ` یک تابع
به‌ازای-هر-گره است، برای هر زوج (گره، core_type) در یک نقطه‌ی نماینده‌ی DVFS (بالاترین سطح
عملکرد به ازای هر نوع هسته) و بردار **میانگین** z_t برازش‌شده روی داده‌ی آموزش (نقطه‌ی عملیاتی
«مورد انتظار/معمول» که یک زمان‌بند آفلاین ایستا استفاده می‌کند) پرسمان می‌شود — از نظر معماری با
نحوه‌ی ساخته‌شدن ۴۲ زمینه‌ی ارزیابی یکسان است، فقط بدون نیاز به یک حقیقت پایه‌ی نمونه‌برداری‌شده.

**زمان‌بندهای پیاده‌سازی‌شده:** HEFT کلاسیک (اولویت‌بندی رتبه‌ی رو-به-بالا + انتخاب پردازنده‌ی
زودترین-زمان-پایان بدون درج) و یک زمان‌بند فهرستی آگاه از موعد زمانی به‌سبک EDF با موعد زمانی
افرازشده (موعدهای فرعی به‌طور متناسب در طول مسیر بحرانی توزیع شده‌اند، گره‌های آماده به ترتیب
صعودی موعد فرعی مرتب می‌شوند).

| زمان‌بند | برآوردکننده‌ی هزینه | DMR | میانگین دیرکرد (میکروثانیه) | میانگین R_end (میکروثانیه) |
|---|---|---|---|---|
| HEFT | Q50 (خوش‌بینانه) | ۰٫۸۴ | ۵٬۲۸۹٬۱۴۸ | ۹٬۴۲۲٬۳۵۵ |
| HEFT | Q95 (خام، کالیبره‌نشده) | ۰٫۹۶ | ۶٬۷۳۲٬۲۸۵ | ۱۱٬۰۱۲٬۴۰۰ |
| HEFT | **C_e (کالیبره‌شده)** | ۰٫۹۸ | ۷٬۴۰۰٬۳۸۸ | ۱۱٬۶۸۵٬۴۰۰ |
| DeadlineAwareList | Q50 (خوش‌بینانه) | ۰٫۸۴ | ۵٬۳۱۱٬۹۶۳ | ۹٬۴۴۱٬۱۱۳ |
| DeadlineAwareList | Q95 (خام، کالیبره‌نشده) | ۰٫۹۶ | ۶٬۷۴۲٬۱۹۱ | ۱۱٬۰۲۱٬۲۳۰ |
| DeadlineAwareList | **C_e (کالیبره‌شده)** | ۰٫۹۸ | ۷٬۴۱۸٬۲۱۸ | ۱۱٬۷۰۲٬۴۲۰ |

*(جدول کامل `results/phase2/scheduling/scheduling_summary.csv`،
`scheduling_summary_by_criticality.csv`؛ نمودار `plots/scheduling_comparison.png`)*

**خوانش درست این اعداد — دو نکته‌ای که مشخصات صراحتاً می‌خواهد:**

۱. **استفاده‌ی تنها از HEFT عدسی ارزیابی بی‌درنگ کافی نیست.** HEFT و زمان‌بند آگاه از موعد
   زمانی در اینجا makespan/DMR تقریباً یکسانی تولید می‌کنند. بررسی چرایی آن (با ارجاع متقابل به
   `metadata/graph_summary.csv`) نشان می‌دهد چندین گراف test_id مقدار `max_width` تا بیش از ۲۰
   در برابر تنها ۴ هسته‌ی فیزیکی دارند — بودجه‌ی موازی‌سازی بستر، نه *سیاست* زمان‌بندی، محدودیت
   الزام‌آور است. یک زمان‌بند صرفاً بهینه‌ساز makespan و یک زمان‌بند آگاه از موعد زمانی دقیقاً
   وقتی تعداد هسته گلوگاه است به یک پاسخ همگرا می‌شوند، که دقیقاً همان وضعیتی است که گزارش تنها
   HEFT/makespan (بدون DMR/دیرکرد) می‌تواند گمراه‌کننده باشد — به همین دلیل این گزارش با DMR و
   دیرکرد آغاز می‌شود و با makespan به‌عنوان یک عدد تأییدکننده‌ی ثانویه رفتار می‌کند، همان‌طور که
   مشخصات می‌خواهد.
۲. **استفاده از `C_e` به‌عنوان هزینه‌ی زمان‌بندی واقعی عمداً بدبینانه است، و این به‌عمد چنین
   است، نه یک نقص.** DMR به‌صورت یکنوا از Q50 به Q95 به C_e افزایش می‌یابد (۰٫۸۴ ← ۰٫۹۶ ←
   ۰٫۹۸) زیرا هر برآوردکننده‌ی بعدی کرانی آماری به‌طور فزاینده محافظه‌کارانه‌تر است، نه یک برآورد
   *مورد انتظار* بهتر. `C_e` برای **تحلیل پذیرش/ایمنی** طراحی شده («آیا این DAG حتی در بدترین
   حالت آماری بدشانس هم قابل‌زمان‌بندی است؟»)، نه به‌عنوان هزینه‌ی واقعی گرایش‌به-توان‌عملیاتی
   که به یک زمان‌بند بهینه‌ساز makespan داده می‌شود. یک سامانه‌ی تولیدی از برآوردهای شبه-Q50/
   میانگین برای *ترتیب و بسته‌بندی زمان‌بندی* استفاده می‌کرد و `C_e` را برای *تصمیم پذیرش و
   بررسی امکان‌پذیری موعد زمانی* نگه می‌داشت — تمایزی که این مقایسه آن را ملموس می‌کند، نه صرفاً
   ادعا.

بر اساس بحرانیت (`scheduling_summary_by_criticality.csv`): DMR بحرانیت HI تحت `C_e` برابر
**۱٫۰۰** است (بدتر از ۰٫۹۶ برای LO) — اصلاح محافظه‌کارانه‌ی HI از بخش ۱ (`q_conf^HI` =
۱۵٬۶۶۶ میکروثانیه) مستقیماً هزینه‌ی پیش‌بینی‌شده‌ی کارهای HI را گسترش می‌دهد، که همان رفتار
ایمنی مورد نظر است اما همچنین به این معناست که، روی این بستر کوچک ۴-هسته‌ای، اساساً هیچ DAG با
بحرانیت HI در این مجموعه‌ی آزمون تحت بودجه‌ی کاملاً محافظه‌کارانه در موعد زمانی اسمی خود
قابل‌زمان‌بندی نیست — یافته‌ای ساختاری مشروع (این بستر نسبت به تقاضای موازی‌سازی این گراف‌ها
کوچک‌تر از نیاز است) نه یک مصنوع برآوردکننده.

---

## ۸. محدودیت‌ها و نکات صداقت

به‌طور صریح مستند شده، منطبق با انتظار مشخصات که خروجی‌های این پروژه به‌عنوان کران‌های آماری
تفسیر شوند، نه حقیقت پایه درباره‌ی سخت‌افزار فیزیکی:

۱. **هیچ بازشبیه‌سازی واقعی سخت‌افزار با بیش از ۴ هسته وجود ندارد.** فشار مقیاس‌پذیری بخش ۳.۲
   تنها `active_core_count` را روی ورودی z_t موجود مدل منجمد بازنویسی می‌کند؛ این کار یک سامانه‌ی
   ۸/۱۶/۳۲ هسته‌ای فیزیکی متفاوت را شبیه‌سازی نمی‌کند (و با توجه به اینکه `core_id` هرگز یک ویژگی
   مدل نبوده، نمی‌تواند). صرفاً به‌عنوان یک مشخصه‌سازی عدم‌قطعیت/استحکام گزارش شده است.
۲. **پروکسی مسیر بحرانی توپولوژیک (مبتنی بر شمارش گام) است، نه وزن‌دار بر اساس سیکل محاسباتی**
   — `topo_level + reverse_topo_level == depth − 1`. یک محاسبه‌ی طولانی‌ترین-مسیر وزن‌دار با
   استفاده از `compute_cycles` تعریف دقیق‌تری خواهد بود؛ پروکسی توپولوژیک یک تقریب استاندارد و
   قابل‌دفاع است اما نباید به‌عنوان «آن» مسیر بحرانی مورد استفاده‌ی هر زمان‌بند خاص بیش‌ازحد
   تعبیر شود.
۳. **روش‌های مبنا (بخش ۵) تحت یک بودجه‌ی محاسباتی کاهش‌یافته آموزش دیدند** (۱۵۰/۳۰ گراف، ۸ دوره،
   CPU) — کافی برای برقراری شکاف‌های جهت‌دار بزرگ و واضح (MLP < GNN-Mean، هر دو به‌طور قابل‌توجه
   زیر مدل منجمد) اما نه برای استخراج نتیجه‌گیری‌های ریزدانه از شکاف‌های کوچک (برای مثال، آزمایش
   حذفی آفلاین-در-برابر-آنلاین z_t، که صراحتاً در این بودجه نتیجه‌گیری‌ناپذیر گزارش شده است).
۴. **زمان‌بندی (بخش ۷) هر DAG را به‌صورت مجزا** روی یک بستر ۴-هسته‌ای بی‌کار ارزیابی می‌کند؛ هیچ
   تداخل چندکاره، قبضه‌شدن یا کنترل پذیرشی مدل نمی‌شود. `Δ_queue` و `Δ_preempt` (رابطه‌ی ۱۶/۴۱
   مشخصات) خارج از دامنه‌ی این تحویل‌شدنی هستند — فقط زمان‌بندی مبتنی بر `Y_exec` ارزیابی می‌شود،
   همان‌طور که هدف اصلی یادگیری مشخصات مشخص می‌کند.
۵. **تمام حقیقت پایه خروجی شبیه‌ساز مصنوعی است** (`y_exec_us`)، نه اندازه‌گیری از سخت‌افزار
   فیزیکی — هر عدد دقتی در این گزارش باید به‌عنوان «مدل چقدر خوب تابع خودِ مولد مصنوعی را بازیابی
   می‌کند» خوانده شود، که تنها ادعای درست و پشتیبانی‌شده توسط این مجموعه‌داده است.
۶. **انتخاب نماینده‌ی DVFS/z_t برای هزینه‌های زمان‌بندی کامل DAG (بخش ۷)** — بالاترین سطح عملکرد
   DVFS به ازای هر نوع هسته و بردار میانگین z_t برازش‌شده روی داده‌ی آموزش. یک انتخاب متفاوت
   (برای مثال، بهینه‌سازی DVFS به‌ازای-هر-گره، یا یک z_t بدبینانه به‌جای میانگین) اعداد مطلق
   DMR/دیرکرد را جابه‌جا می‌کند؛ ترتیب *نسبی* در سرتاسر برآوردکننده‌های هزینه (Q50 < Q95 < C_e از
   نظر محافظه‌کاری) یافته‌ی استوار و مستقل‌از-برآوردکننده‌ای است که این گزارش بر آن تکیه می‌کند.
۷. **هیچ زمان‌اجرای زنده‌ی چندحالته‌ی بحرانیت آمیخته شبیه‌سازی نشده است.** نرخ تحریک
   تعویض‌حالت بخش ۶.۳ و بحث کیفی تأثیر آن بر سرویس LO از آمار فراروی ایستا به ازای هر رکورد
   استخراج شده‌اند، نه از یک شبیه‌سازی واقعی زمان‌بند تعویض‌حالت آنلاین با تنزل/کنارگذاشتن کار LO.
۸. **رهاسازی بارانی کل کارهای DAG خارج از دامنه است** (فرض زمان‌بندی مجزای تک-DAG بخش ۷ از قبل
   آن را حذف می‌کند)؛ فقط پروکسی‌های بارانی‌بودن درون-DAG (`release_jitter_us`، به‌عنوان بخشی از
   آزمون فشار بار دنباله‌ای ترکیبی در بخش ۶.۲) پوشش داده شده‌اند.

---

## ۹. فهرست مصنوعات

| مسیر | محتوا |
|---|---|
| `models/phase2/calibration/` | `conformal_config.json`, `conformal_state.json` (منجمد) |
| `results/phase2/calibration/` | پیش‌بینی‌های خام + کالیبره‌شده، گزارش‌های پوشش/تورم، پویش حساسیت α/δ، توزیع امتیاز عدم‌انطباق |
| `results/phase2/ood/` | test_id در برابر test_ood، تشخیص‌های گروه‌بندی‌شده (هسته/DVFS/بحرانیت/نقش/اندازه‌ی گراف/ساختار/مسیر بحرانی/operation_type)، تحلیل تعویض‌حالت بحرانیت آمیخته، زمان استنتاج در برابر اندازه‌ی گراف، همبستگی نمایش نهفته‌ی GNN/خطا (`src/phase2/embeddings.py`, `scripts/run_phase2_deepdive.py`) |
| `results/phase2/scalability/` | معیارهای core_4/8/16/32 + خلاصه |
| `results/phase2/drift/` | فشار بار دنباله‌ای، اضافه‌بار مصنوعی، کالیبراسیون ایستا در برابر پنجره‌ی لغزان |
| `results/phase2/scheduling/` | نتایج HEFT + آگاه از موعد زمانی، DMR/دیرکرد/اسلک |
| `results/phase2/baselines/` | مدل‌های MLP / GNN-Mean / GNN-Mean-NoZt + جدول مقایسه‌ای |
| `results/phase2/plots/` | تمام ۱۳ نموداری که در بالا ارجاع داده شده |
| `notebooks/03_phase2_calibration_scheduling.ipynb` | گام‌به‌گام قابل‌اجرا و روایت‌شده |
